set ARCH=&& set OSTYPE=pc-windows&& set CLIBTYPE=msvc&& (if "%PROCESSOR_ARCHITECTURE%"=="ARM64" set ARCH=aarch64) && (if "%PROCESSOR_ARCHITECTURE%"=="AMD64" set ARCH=x86_64) && (if "%PROCESSOR_ARCHITECTURE%"=="x86" ((if not defined PROCESSOR_ARCHITEW6432 set ARCH=i686) && (if "%PROCESSOR_ARCHITEW6432%"=="AMD64" set ARCH=x86_64))) && (if defined PROCESSOR_ARCHITEW6432 set OSTYPE=%OSTYPE%-gnu) && (if not defined ARCH ( echo Unsupported CPU architecture 1>&2 && exit /b 1)) && call echo %ARCH%-%OSTYPE%-%CLIBTYPE%